const USERNAME = 'alma-2020';
const IS_PROD = process.env.NODE_ENV === 'production'

/*
module.exports = {
    assetPrefix: IS_PROD ? `/${USERNAME}/` : '',
    basePath: IS_PROD ? `/${USERNAME}` : '',
}
*/
