const PROJECT_NAME = 'alma-2020';
const IS_PROD = process.env.NODE_ENV === 'production'

/*
module.exports = {
    assetPrefix: IS_PROD ? `/${PROJECT_NAME}/` : '',
    basePath: IS_PROD ? `/${PROJECT_NAME}` : '',
}
*/

module.exports = {
}